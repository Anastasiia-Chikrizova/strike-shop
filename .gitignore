# Dependencies
node_modules
.pnp
.pnp.js

# Yarn 2+
.yarn/*
.yarn
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# Testing
coverage

# Next.js
**/.next/
**/out/

# TypeScript incremental build cache
*.tsbuildinfo

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# tfvars may hold real values; the .example templates are tracked.
*.tfvars
*.tfvars.json
!*.tfvars.example
# Saved plans hold resource values in cleartext.
tfplan
*.tfplan
# .terraform.lock.hcl is deliberately NOT ignored in root modules — it pins
# provider hashes, same role as package-lock.json. Reusable modules do not
# consume a lock file, so theirs is just init noise.
infra/terraform/modules/*/.terraform.lock.hcl

# Production
**/build
**/dist

# Misc
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
# Any *.env, not just dotfiles: real values end up in names like
# backend.env or ff.env. The .example templates are tracked (they do not
# end in .env).
*.env
**/.env
**/.env.local
**/.env.development.local
**/.env.test.local
**/.env.production.local

# Turbo
.turbo
.vscode

# vercel
.vercel

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
**/.DS_Store

.idea
**/.env.*
!**/.env.template

# Agent tooling — personal setup, not shared with the repo
.claude/
.agents/
